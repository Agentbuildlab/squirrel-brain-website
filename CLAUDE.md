# CLAUDE.md — Squirrel Brain marketing site

This is the **website** repo for the Squirrel Brain **website/promo Claude** (NOT the app-code
session). The canonical operating doc + durable truth live in the sibling **promo** repo:

- **`~/Projects/squirrel-brain-promo/CLAUDE.md`** — how I work (protocols, pipeline, the creative rule).
- **`~/Projects/squirrel-brain-promo/memory.md`** — durable truth + current state + asset inventory.
- App product facts (read-only, other session): `/Volumes/ClawDrive/Adam/squirrel-brain/memory.md`.

**Read those first.** Site specifics: Next.js → **Vercel** (`npx vercel --prod --yes`), domain
`squirrelbrainapp.com` (we own this ONLY — never reference `squirrelbrain.com`). Hero/demo videos
are served from `public/preview/*.mp4`; `/demos` = `app/demos/page.tsx` (3 color lanes). Local
working notes: `HANDOFF.md`, `CODEMAP.md`. Pricing: the site's two tiers (Standard $9.99 / Plus
$14.99) are correct (Adam confirmed 2026-06-16); the app repo's one-plan note is stale — leave it.
